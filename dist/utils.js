// Generated by CoffeeScript 1.6.3
(function() {
  var clone, deepCloneObject, extend, log, uniqueId, __Clone;

  __Clone = function() {};

  clone = function(obj) {
    __Clone.prototype = obj;
    return new __Clone;
  };

  deepCloneObject = function(obj) {
    var key, temp, value;
    if ((obj === null) || (typeof obj !== 'object')) {
      return obj;
    } else {
      temp = {};
      for (key in obj) {
        value = obj[key];
        temp[key] = deepCloneObject(value);
      }
      return temp;
    }
  };

  extend = function(target, source, fnCanCopy) {
    var key, val;
    for (key in source) {
      val = source[key];
      if ((!target.hasOwnProperty(key)) && ((!fnCanCopy) || fnCanCopy(key))) {
        target[key] = val;
      }
    }
    return target;
  };

  uniqueId = function(length) {
    var id;
    if (length == null) {
      length = 24;
    }
    id = "";
    while (id.length < length) {
      id += Math.random().toString(36).substr(2);
    }
    return id.substr(0, length);
  };

  log = function(msg) {
    return console.log(msg);
  };

  exports.clone = clone;

  exports.deepCloneObject = deepCloneObject;

  exports.extend = extend;

  exports.uniqueId = uniqueId;

  exports.log = log;

}).call(this);
